= form_with(model: [post, comment], local: true) do |f|
  .space-y-4
    .mb-4
      = f.rich_text_area :body, class: "w-full p-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-500"      
    .mt-4
      = f.submit submit_label, class: "bg-blue-500 text-white py-2 px-4 rounded mt-1 hover:bg-blue-700"

