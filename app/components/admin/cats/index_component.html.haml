%h1 Lista Kotów

%table
  %thead
    %tr
      %th ID
      %th Imię
      %th Rasa
      %th Kolor
      %th Data urodzenia
      %th Płeć
      %th Cena
      %th Status
      %th Lokalizacja
      %th Notatki
      %th Akcje

  %tbody
    - @cats.each do |cat|
      %tr
        %td= cat.id
        %td= cat.name
        %td= cat.breed
        %td= cat.color
        %td= cat.date_of_birth
        %td= cat.gender
        %td= cat.price
        %td= cat.status
        %td= cat.location
        %td= cat.notes        
        %td
          - if cat.reservation_reported?
            = button_to "zatwierdz rezerwacje", accept_reservation(cat: cat), method: :put
          -# = link_to 'Show', cat
          |
          -# = link_to 'Edytuj', edit_cat_path(cat)
          |
          -# = link_to 'Usuń', cat, method: :delete, data: { confirm: 'Czy na pewno?' }

= link_to 'New Cat', new_admin_cat_path
